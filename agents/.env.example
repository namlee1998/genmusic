# Custom LLM Provider (OpenAI-compatible API)
OPENAI_API_KEY=
OPENAI_API_BASE=
DEFAULT_MODEL=
PO_MODEL=kr/claude-sonnet-4.5
UX_MODEL=gpt-4o
QA_MODEL=deepseek-v4-pro
DEV_FALLBACK_MODEL=kr/claude-sonnet-4.5

# Server
PORT=8001
HOST=0.0.0.0

# Logging
LOG_LEVEL=INFO

# Langfuse tracing for the Python agents service (optional)
LANGFUSE_ENABLED=false
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

# Use the host matching your Langfuse project region.
LANGFUSE_HOST=https://cloud.langfuse.com

# Optional DEV patch sandbox. Generated diffs are applied in an isolated Git
# worktree before the configured commands run.
AGENT_REAL_SANDBOX=false
AGENT_WORKSPACE_REPO=..
AGENT_TEST_COMMANDS=python -m pytest -q
AGENT_TEST_TIMEOUT_SECONDS=300
AGENT_COMMIT_PATCH=true
AGENT_KEEP_WORKTREE=true

# Optional E2B DEV execution path. Keep disabled for local fallback mode.
USE_E2B_SANDBOX=false
E2B_API_KEY=
ANTHROPIC_API_KEY=
