# --- Core libs ---
fastapi
starlette>=0.37.2,<0.38.0
uvicorn[standard]>=0.27.0,<1.0.0
fastapi==0.110.0


h11>=0.14.0
huggingface_hub
click>=8.0.0   # đảm bảo có click (bổ sung rõ ràng)

# --- HF stack (pinned) ---
transformers==4.37.2
accelerate>=0.21.0,<1.0.0   # tránh yêu cầu device_mesh (cần torch>=2.2)

# --- Audio / Music ---
audiocraft==1.3.0
pydub
scipy>=1.10,<1.14

# --- NumPy (an toàn cho SciPy/audiocraft) ---
numpy<2

# --- Uploads ---
python-multipart

# --- Bark (git) ---
git+https://github.com/suno-ai/bark.git
